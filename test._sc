(defun c:main ()
  (setq osmodo (getvar "osmode"))
  (setvar "osmode" 0)
  (command "layer" "s" "0" "")
  (command "ucs" "w")
  ;;entrada de datos
  (setq puntoin (getpoint "Punto seleccionado: \n"))

  (command "circle" "d" 0.5)
  (setq circu (ENTLAST))
  
  (command "pline")

(setvar "osmode" osmodo)
)
